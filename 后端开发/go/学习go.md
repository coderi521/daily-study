---
annotation-target: resource/go-study.pdf
annotation-target-type: pdf
---


>%%
>```annotation-json
>{"created":"2026-08-14T08:51:06.667Z","text":"`if`、`else`：条件判断\n`switch`、`case`、`default`：多分支选择\n`fallthrough`：switch 穿透，强制执行下一个 case\n`for`：循环（Go 没有 while，只用 for）\n`continue`：继续下一轮循环\n`break`：跳出循环 /switch\n`return`：函数返回\n`goto`：无条件跳转（尽量少用）\n`var`：定义变量\n`const`：定义常量\n`type`：自定义类型\n`struct`：结构体\n`map`：map 集合类型\n`func`：定义函数\n`interface`：接口\n`package`：声明当前属于哪个包\n`import`：导入其他包\n`go`：启动 goroutine 协程\n`defer`：延迟执行，函数退出前运行\n`select`：多路监听 channel\n`chan`：通道，goroutine 之间通信\n`range`：遍历数组、切片、map、channel\n","updated":"2026-08-14T08:51:06.667Z","document":{"title":"go-study.pdf","link":[{"href":"urn:x-pdf:02cb2f11d27adc1f301daee1fcc0f851"},{"href":"vault:/resource/go-study.pdf"}],"documentFingerprint":"02cb2f11d27adc1f301daee1fcc0f851"},"uri":"vault:/resource/go-study.pdf","target":[{"source":"vault:/resource/go-study.pdf","selector":[{"type":"TextPositionSelector","start":3597,"end":3600},{"type":"TextQuoteSelector","exact":"关键字","prefix":"        C语言中一共有32个关键字Go语言中一共有25个","suffix":"数据类型对比  C语言数据类型微信公众号代码情缘类型 32位编译"}]}]}
>```
>%%
>*%%PREFIX%%C语言中一共有32个关键字Go语言中一共有25个%%HIGHLIGHT%% ==关键字== %%POSTFIX%%数据类型对比  C语言数据类型微信公众号代码情缘类型 32位编译*
>%%LINK%%[[#^8b116w5o0b2|show annotation]]
>%%COMMENT%%
>`if`、`else`：条件判断
>`switch`、`case`、`default`：多分支选择
>`fallthrough`：switch 穿透，强制执行下一个 case
>`for`：循环（Go 没有 while，只用 for）
>`continue`：继续下一轮循环
>`break`：跳出循环 /switch
>`return`：函数返回
>`goto`：无条件跳转（尽量少用）
>`var`：定义变量
>`const`：定义常量
>`type`：自定义类型
>`struct`：结构体
>`map`：map 集合类型
>`func`：定义函数
>`interface`：接口
>`package`：声明当前属于哪个包
>`import`：导入其他包
>`go`：启动 goroutine 协程
>`defer`：延迟执行，函数退出前运行
>`select`：多路监听 channel
>`chan`：通道，goroutine 之间通信
>`range`：遍历数组、切片、map、channel
>
>%%TAGS%%
>
^8b116w5o0b2
