---
annotation-target: resource/深入理解-AI-Agent-李博杰-v1.1.pdf
annotation-target-type: pdf
---


>%%
>```annotation-json
>{"created":"2026-08-15T10:51:40.624Z","text":"# 整体结构图解读\n\n这是一张**带事件队列的异步 Agent 智能体架构流程图**，整套系统分为三大模块：**事件源 → 事件队列 → Agent 处理流程**。\n\n## 一、第一部分：事件源（产生任务的入口）\n\n也就是消息是从哪里来的，图中 4 种触发来源：\n\n1. **Email（邮件）**：收到邮件回复时触发事件 `on_email_reply`，附带发件人、邮件主题等数据\n2. **Timer（定时器）**：定时任务到期触发 `on_timer_expire`，例如每天早上 9 点自动生成日报\n3. **Webhook（外部回调）**：第三方系统推送回调，例如代码仓库 PR 合并后发送通知事件\n4. **User（用户）**：用户直接发消息，例如：`帮我查下明天的天气`\n\n> \n> 所有来源产生的消息，**不会立刻交给大模型处理**，而是先送入「事件队列」。\n\n## 二、第二部分：事件队列（任务缓冲区 + 优先级调度）\n\n队列相当于一个待办任务清单，具备**优先级机制**：\n\n- `user.input`：用户输入，常规优先级\n- `email.reply`：邮件回复，常规优先级\n- `user.interrupt`：用户紧急中断消息，**紧急优先级（高优插队）**\n- `timer.trigger`：定时触发任务，常规优先级\n\n核心能力：\n\n1. **缓冲削峰**：大量事件同时涌入不会直接压垮 Agent；\n2. **优先级调度**：紧急事件可以优先被取出处理；\n3. **异步解耦**：事件产生方不用等待 Agent 立刻处理，发完即可返回。\n\n队列会**按优先级取出事件**，送入右侧 Agent 流水线。\n\n## 三、第三部分：Agent 处理流程（智能体执行闭环）\n\n一条事件被取出后，依次经过 5 个阶段，并且支持循环思考：\n\n### 1. 路由器\n\n由 LLM（大模型）判定这条事件的紧急程度、任务类型，做任务分发路由。\n\n### 2. 追加到轨迹\n\n把当前事件转成结构化数据，存入会话 / 任务轨迹（上下文记忆），保证 Agent 知道完整历史。\n\n### 3. LLM 推理（Agent 核心循环）\n\n执行经典 **观察→思考→行动 (Observation‑Thought‑Action)** 的 Agent 循环。\n\n> \n> 虚线箭头代表循环：如果大模型认为还没做完，可以再次回到路由器继续多轮思考，直到任务完成。\n\n### 4. 工具执行\n\n大模型思考后决定调用工具，工具可以同步执行（立刻返回）或者异步执行（耗时任务），例如查天气、调用接口、读写文件。\n\n### 5. 结果处理\n\n任务完成后，对结果做收尾：给用户回复通知、发送邮件、把结果存入数据库。\n\n# 数据流完整示例（一条链路跑通）\n\n> \n> 用户发送紧急消息 → 进入队列标记紧急优先级 → 优先取出 → 路由器判定紧急任务 → 添加到轨迹上下文 → LLM 多轮推理 → 调用工具查询 → 处理结果通知用户\n\n# 架构亮点总结\n\n1. **异步事件驱动**：Agent 不是一直等待用户，可被邮件、定时器、webhook 等后台事件唤醒；\n2. **优先级中断能力**：紧急消息可以插队；\n3. **标准 Agent 循环**：支持多轮思考、工具调用；\n4. **生产消费解耦**：事件生产者（邮件 / 定时器）和消费者（Agent）完全分开。","updated":"2026-08-15T10:51:40.624Z","document":{"title":"深入理解 AI Agent：设计原理与工程实践","link":[{"href":"urn:x-pdf:687158e7ea7c5dc223445a4481b5f9f5"},{"href":"vault:/resource/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3-AI-Agent-%E6%9D%8E%E5%8D%9A%E6%9D%B0-v1.1.pdf"}],"documentFingerprint":"687158e7ea7c5dc223445a4481b5f9f5"},"uri":"vault:/resource/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3-AI-Agent-%E6%9D%8E%E5%8D%9A%E6%9D%B0-v1.1.pdf","target":[{"source":"vault:/resource/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3-AI-Agent-%E6%9D%8E%E5%8D%9A%E6%9D%B0-v1.1.pdf","selector":[{"type":"TextPositionSelector","start":189659,"end":189680},{"type":"TextQuoteSelector","exact":"事件队列与 Agent 处理流程之间的关系","prefix":"4-3 给出了事件驱动异步 Agent 的整体架构，展示事件源、","suffix":"。事件源Emailon_email_reply{\"from\":\""}]}]}
>```
>%%
>*%%PREFIX%%4-3 给出了事件驱动异步 Agent 的整体架构，展示事件源、%%HIGHLIGHT%% ==事件队列与 Agent 处理流程之间的关系== %%POSTFIX%%。事件源Emailon_email_reply{"from":"*
>%%LINK%%[[#^xuyw2673zio|show annotation]]
>%%COMMENT%%
># 整体结构图解读
>
>这是一张**带事件队列的异步 Agent 智能体架构流程图**，整套系统分为三大模块：**事件源 → 事件队列 → Agent 处理流程**。
>
>## 一、第一部分：事件源（产生任务的入口）
>
>也就是消息是从哪里来的，图中 4 种触发来源：
>
>1. **Email（邮件）**：收到邮件回复时触发事件 `on_email_reply`，附带发件人、邮件主题等数据
>2. **Timer（定时器）**：定时任务到期触发 `on_timer_expire`，例如每天早上 9 点自动生成日报
>3. **Webhook（外部回调）**：第三方系统推送回调，例如代码仓库 PR 合并后发送通知事件
>4. **User（用户）**：用户直接发消息，例如：`帮我查下明天的天气`
>
>> 
>> 所有来源产生的消息，**不会立刻交给大模型处理**，而是先送入「事件队列」。
>
>## 二、第二部分：事件队列（任务缓冲区 + 优先级调度）
>
>队列相当于一个待办任务清单，具备**优先级机制**：
>
>- `user.input`：用户输入，常规优先级
>- `email.reply`：邮件回复，常规优先级
>- `user.interrupt`：用户紧急中断消息，**紧急优先级（高优插队）**
>- `timer.trigger`：定时触发任务，常规优先级
>
>核心能力：
>
>1. **缓冲削峰**：大量事件同时涌入不会直接压垮 Agent；
>2. **优先级调度**：紧急事件可以优先被取出处理；
>3. **异步解耦**：事件产生方不用等待 Agent 立刻处理，发完即可返回。
>
>队列会**按优先级取出事件**，送入右侧 Agent 流水线。
>
>## 三、第三部分：Agent 处理流程（智能体执行闭环）
>
>一条事件被取出后，依次经过 5 个阶段，并且支持循环思考：
>
>### 1. 路由器
>
>由 LLM（大模型）判定这条事件的紧急程度、任务类型，做任务分发路由。
>
>### 2. 追加到轨迹
>
>把当前事件转成结构化数据，存入会话 / 任务轨迹（上下文记忆），保证 Agent 知道完整历史。
>
>### 3. LLM 推理（Agent 核心循环）
>
>执行经典 **观察→思考→行动 (Observation‑Thought‑Action)** 的 Agent 循环。
>
>> 
>> 虚线箭头代表循环：如果大模型认为还没做完，可以再次回到路由器继续多轮思考，直到任务完成。
>
>### 4. 工具执行
>
>大模型思考后决定调用工具，工具可以同步执行（立刻返回）或者异步执行（耗时任务），例如查天气、调用接口、读写文件。
>
>### 5. 结果处理
>
>任务完成后，对结果做收尾：给用户回复通知、发送邮件、把结果存入数据库。
>
># 数据流完整示例（一条链路跑通）
>
>> 
>> 用户发送紧急消息 → 进入队列标记紧急优先级 → 优先取出 → 路由器判定紧急任务 → 添加到轨迹上下文 → LLM 多轮推理 → 调用工具查询 → 处理结果通知用户
>
># 架构亮点总结
>
>1. **异步事件驱动**：Agent 不是一直等待用户，可被邮件、定时器、webhook 等后台事件唤醒；
>2. **优先级中断能力**：紧急消息可以插队；
>3. **标准 Agent 循环**：支持多轮思考、工具调用；
>4. **生产消费解耦**：事件生产者（邮件 / 定时器）和消费者（Agent）完全分开。
>%%TAGS%%
>
^xuyw2673zio


>%%
>```annotation-json
>{"created":"2026-08-15T11:01:34.505Z","text":"这一节讲的是**Agent 智能体怎么和人类用户聊天交互**，核心是区分「老式一问一答对话」和「新一代异步秘书式沟通」两种完全不同的人机交互范式。\n\n## 一、先看懂老一代 Agent 的沟通范式（Claude Code、Manus、Genspark 这类）\n\n> \n> 原生 ReAct 循环，用户发一条，Agent 回一条\n\n### 特点\n\n1. **会话（session）强绑定**\n所有对话被锁死在一个固定会话窗口里，你必须打开这个 App、找到对应的聊天会话页面，才能看见 Agent 的回复。\n2. **严格的回合制同步对话**：`用户输入 → Agent思考+调用工具 → Agent输出回复`，一轮一轮交替。\nAgent 所有想对你说的话，直接作为大模型（assistant）消息返回在聊天框，没有额外发送通道。\n3. **Agent 不能主动找你**\n只有你先发消息唤醒 Agent，Agent 才能回复；Agent 在后台跑长任务的时候，中途没办法主动发消息通知你。\n\n举个例子：\n你给 Agent 下达任务：帮我分析一份报表，需要运行 20 分钟。\n👉 老式 Agent：你只能停留在这个聊天页面干等结果；你关掉页面之后，任务跑完了你收不到任何通知。\n\n## 二、新范式代表：OpenClaw（异步、透明会话）\n\n它打破了上面一问一答的限制，也就是文档所说「session 对用户透明」。\n\n### 核心特性拆解\n\n1. **你不需要感知会话 (session)**\n你不用专门打开某个聊天窗口。Agent 的后台任务、工具调用过程全部对你隐藏。你不需要关心 Agent 正在后台调用了哪些工具。\n2. **双向随时发消息，不再是回合制**\n   - ✅ 用户可以中途随时插一条消息打断 Agent\n   - ✅ **Agent 也可以主动给你发消息**，不需要等你先发消息\n3. **异步沟通，像真人秘书**\n不需要双方同时在线。就像微信消息：你发完消息可以关掉页面去干别的；Agent 后台慢慢干活，干完主动推送结果给你。这也就是文中所说的「活人感」。\n\n> \n> 关键点：Agent 不再直接把大模型的文字输出扔到聊天框。**而是调用一个专门的「用户沟通工具」来发送消息**。\n\n### 这个沟通工具可以发送的内容\n\n- 普通文本消息\n- 附件：图片、文件\n- 带优先级的推送提醒：紧急任务立刻弹窗通知，普通任务晚点推送\n\n## 三、更多扩展：多模态 / 生成式 UI 沟通能力\n\n除了纯文字消息，Agent 还可以通过沟通工具发送更丰富的内容：\n\n1. **结构化卡片消息**：比如任务进度卡片、报表卡片，而不是一大段纯文字\n2. **跨渠道通知**：给你发提醒邮件、短信\n3. **生成式 UI（交互式界面）**：Agent 直接生成一段 HTML 交互页面。\n\n> \n> 举例子：Agent 做完数据分析之后，不只是发文字数字，而是生成一个可以交互的图表网页给你查看。\n\n## 四、架构层面，「用户沟通工具」的硬性设计要求\n\n文档最后列出 4 个设计目标：\n\n1. **支持异步消息模式**：用户不在线的时候 Agent 也能发消息，消息先存起来，等用户上线再看到\n2. **已读 / 未读追踪**：Agent 可以知道你有没有看到这条消息；你没读的紧急消息可以二次推送提醒\n3. **多渠道消息一致性**：如果你同时开启微信、邮件、App 弹窗多个接收渠道，同一条通知在所有渠道状态同步，不会重复骚扰\n\n# 📌新旧模式对比表\n\n表格\n\n| 维度 | 传统 ReAct Agent（回合对话） | 带用户沟通工具的新一代 Agent（OpenClaw 范式） |\n| --- | --- | --- |\n| 对话模式 | 同步回合制，一问一答 | 异步双向，随时互发消息 |\n| 会话 Session | 用户必须感知、进入对应会话窗口 | 后台会话透明隐藏，用户无感知 |\n| Agent 主动性 | 不能主动发消息，只能被动回复 | 后台任务完成后主动推送消息给用户 |\n| 消息载体 | 只能输出大模型文本 | 文本、图片、文件、卡片、HTML 交互页面、邮件、推送通知 |\n| 使用场景 | 短对话，即时问答 | 长时间后台任务、秘书助手、异步工作流 |\n\n# 通俗总结\n\n以前 Agent 就像网页版 ChatGPT，你必须守在聊天窗口一问一答。\n而**用户沟通工具就是给 Agent 装上了「微信 / 消息推送能力」**，让它变成一个可以在后台默默干活，干完主动发消息通知你的秘书，并且通知还能走邮件、弹窗、卡片等多种渠道。","updated":"2026-08-15T11:01:34.505Z","document":{"title":"深入理解 AI Agent：设计原理与工程实践","link":[{"href":"urn:x-pdf:687158e7ea7c5dc223445a4481b5f9f5"},{"href":"vault:/resource/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3-AI-Agent-%E6%9D%8E%E5%8D%9A%E6%9D%B0-v1.1.pdf"}],"documentFingerprint":"687158e7ea7c5dc223445a4481b5f9f5"},"uri":"vault:/resource/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3-AI-Agent-%E6%9D%8E%E5%8D%9A%E6%9D%B0-v1.1.pdf","target":[{"source":"vault:/resource/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3-AI-Agent-%E6%9D%8E%E5%8D%9A%E6%9D%B0-v1.1.pdf","selector":[{"type":"TextPositionSelector","start":192628,"end":192634},{"type":"TextQuoteSelector","exact":"用户沟通工具","prefix":"查询的次数。1144.7 事件驱动的异步 Agent4.7.4 ","suffix":"用户沟通工具是在 Agent 与用户的沟通渠道日益多元化的情况下"}]}]}
>```
>%%
>*%%PREFIX%%查询的次数。1144.7 事件驱动的异步 Agent4.7.4%%HIGHLIGHT%% ==用户沟通工具== %%POSTFIX%%用户沟通工具是在 Agent 与用户的沟通渠道日益多元化的情况下*
>%%LINK%%[[#^98i768tsnk8|show annotation]]
>%%COMMENT%%
>这一节讲的是**Agent 智能体怎么和人类用户聊天交互**，核心是区分「老式一问一答对话」和「新一代异步秘书式沟通」两种完全不同的人机交互范式。
>
>## 一、先看懂老一代 Agent 的沟通范式（Claude Code、Manus、Genspark 这类）
>
>> 
>> 原生 ReAct 循环，用户发一条，Agent 回一条
>
>### 特点
>
>1. **会话（session）强绑定**
>所有对话被锁死在一个固定会话窗口里，你必须打开这个 App、找到对应的聊天会话页面，才能看见 Agent 的回复。
>2. **严格的回合制同步对话**：`用户输入 → Agent思考+调用工具 → Agent输出回复`，一轮一轮交替。
>Agent 所有想对你说的话，直接作为大模型（assistant）消息返回在聊天框，没有额外发送通道。
>3. **Agent 不能主动找你**
>只有你先发消息唤醒 Agent，Agent 才能回复；Agent 在后台跑长任务的时候，中途没办法主动发消息通知你。
>
>举个例子：
>你给 Agent 下达任务：帮我分析一份报表，需要运行 20 分钟。
>👉 老式 Agent：你只能停留在这个聊天页面干等结果；你关掉页面之后，任务跑完了你收不到任何通知。
>
>## 二、新范式代表：OpenClaw（异步、透明会话）
>
>它打破了上面一问一答的限制，也就是文档所说「session 对用户透明」。
>
>### 核心特性拆解
>
>1. **你不需要感知会话 (session)**
>你不用专门打开某个聊天窗口。Agent 的后台任务、工具调用过程全部对你隐藏。你不需要关心 Agent 正在后台调用了哪些工具。
>2. **双向随时发消息，不再是回合制**
>   - ✅ 用户可以中途随时插一条消息打断 Agent
>   - ✅ **Agent 也可以主动给你发消息**，不需要等你先发消息
>3. **异步沟通，像真人秘书**
>不需要双方同时在线。就像微信消息：你发完消息可以关掉页面去干别的；Agent 后台慢慢干活，干完主动推送结果给你。这也就是文中所说的「活人感」。
>
>> 
>> 关键点：Agent 不再直接把大模型的文字输出扔到聊天框。**而是调用一个专门的「用户沟通工具」来发送消息**。
>
>### 这个沟通工具可以发送的内容
>
>- 普通文本消息
>- 附件：图片、文件
>- 带优先级的推送提醒：紧急任务立刻弹窗通知，普通任务晚点推送
>
>## 三、更多扩展：多模态 / 生成式 UI 沟通能力
>
>除了纯文字消息，Agent 还可以通过沟通工具发送更丰富的内容：
>
>1. **结构化卡片消息**：比如任务进度卡片、报表卡片，而不是一大段纯文字
>2. **跨渠道通知**：给你发提醒邮件、短信
>3. **生成式 UI（交互式界面）**：Agent 直接生成一段 HTML 交互页面。
>
>> 
>> 举例子：Agent 做完数据分析之后，不只是发文字数字，而是生成一个可以交互的图表网页给你查看。
>
>## 四、架构层面，「用户沟通工具」的硬性设计要求
>
>文档最后列出 4 个设计目标：
>
>1. **支持异步消息模式**：用户不在线的时候 Agent 也能发消息，消息先存起来，等用户上线再看到
>2. **已读 / 未读追踪**：Agent 可以知道你有没有看到这条消息；你没读的紧急消息可以二次推送提醒
>3. **多渠道消息一致性**：如果你同时开启微信、邮件、App 弹窗多个接收渠道，同一条通知在所有渠道状态同步，不会重复骚扰
>
># 📌新旧模式对比表
>
>表格
>
>| 维度 | 传统 ReAct Agent（回合对话） | 带用户沟通工具的新一代 Agent（OpenClaw 范式） |
>| --- | --- | --- |
>| 对话模式 | 同步回合制，一问一答 | 异步双向，随时互发消息 |
>| 会话 Session | 用户必须感知、进入对应会话窗口 | 后台会话透明隐藏，用户无感知 |
>| Agent 主动性 | 不能主动发消息，只能被动回复 | 后台任务完成后主动推送消息给用户 |
>| 消息载体 | 只能输出大模型文本 | 文本、图片、文件、卡片、HTML 交互页面、邮件、推送通知 |
>| 使用场景 | 短对话，即时问答 | 长时间后台任务、秘书助手、异步工作流 |
>
># 通俗总结
>
>以前 Agent 就像网页版 ChatGPT，你必须守在聊天窗口一问一答。
>而**用户沟通工具就是给 Agent 装上了「微信 / 消息推送能力」**，让它变成一个可以在后台默默干活，干完主动发消息通知你的秘书，并且通知还能走邮件、弹窗、卡片等多种渠道。
>%%TAGS%%
>
^98i768tsnk8
